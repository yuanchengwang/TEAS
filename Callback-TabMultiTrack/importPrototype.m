function importProtocol(hObject,eventData)
%Import a .m file which define the function of output mode
%The protocol has the priority
msgbox('Protocol setting for playing technique highly related to the synthesizer will be supported in the future.');
%.m/.fxp
%     [fileNameSuffix,filePath] = uigetfile('*.m','Select File');
%     
%     if isnumeric(fileNameSuffix) == 0
%         addpath(filePath);
%         eval(fileNameSuffix(1:end-2));
%     end
end